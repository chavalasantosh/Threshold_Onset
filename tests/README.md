# Project Tests

Root-level tests for THRESHOLD_ONSET.

| File | Purpose |
|------|---------|
| `test_phase3_convergence.py` | Phase 3 convergence test |
| `test_phase4_freeze.py` | Phase 4 freeze validation |

Run: `python -m pytest tests/ -v` or `python main.py` (or `python run_all.py`) for full project run.
